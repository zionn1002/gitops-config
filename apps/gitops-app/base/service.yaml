apiVersion: v1
kind: Service
metadata:
  name: gitops-app-svc
  labels:
    app: gitops-app
spec:
  type: ClusterIP
  selector:
    app: gitops-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
